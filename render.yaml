services:
  - type: web
    name: mongodb-tiny-social-media-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production